{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities required by the HQ Git main window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
